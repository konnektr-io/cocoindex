[project]
name = "meeting-notes-graph-age"
version = "0.1.0"
description = "CocoIndex example: build a knowledge graph from meeting notes in Google Drive, stored in Apache AGE."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[age,google_drive,sentence_transformers,entity_resolution_llm]>=1.0.2",
    "instructor",
    "litellm",
    "pydantic",
]

[tool.setuptools]
py-modules = ["main"]
