[project]
name = "docs-to-kg-age"
version = "0.1.0"
description = "Simple example for cocoindex: extract triples from files and build knowledge graph using Apache AGE."
requires-python = ">=3.11"
dependencies = ["cocoindex>=0.3.9"]

[tool.setuptools]
packages = []
